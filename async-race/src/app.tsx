import { Popik } from '~/engine/popik.ts';

export const App = (): JSX.Element => {
  // const [counter, setCounter] = Popik.useState(0);
  // const garageModel = createGarageModel();
  // const cars = garageModel.getCars();
  //
  return <Popik.Fragment>{/*<CarContainer />*/}</Popik.Fragment>;
};
